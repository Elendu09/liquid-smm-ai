import { ReshareStudio } from "@/components/engage/ReshareStudio";

/** Engage → Reshare: the cross-platform delivery control plane. */
export default function ReshareStudioView() {
  return <ReshareStudio />;
}
